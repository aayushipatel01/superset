<!--
PLACEHOLDER — replace the entire body below with the verbatim master
scan-orchestrator prompt text (manual variant, i.e. with
trigger_source = "manual" hardcoded).

This file is passed verbatim as the `prompt` field of the Devin
`POST /v3/organizations/{org_id}/sessions` call made by
`.github/workflows/devin-scan-orchestrator.yml`. It is deliberately NOT a
playbook: the workflow embeds the text itself so the prompt is versioned
alongside the workflow that dispatches it.

The workflow appends a short, machine-generated "Run context" block to the
end of this text (github run id, run url, max_parallel, target repo). Do not
duplicate those values here.
-->

# Master scan orchestrator (manual)

trigger_source = "manual"

TODO: paste the master scan-orchestrator prompt text here.
